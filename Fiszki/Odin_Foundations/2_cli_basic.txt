Co to jest Command Line (CLI)?;Interfejs tekstowy, w którym wpisujesz polecenia, które komputer ma wykonać.;odin_cli
Co oznacza symbol znaku zachęty (Prompt) - $ lub %?;Oznacza, że terminal oczekuje na wpisanie polecenia.;odin_cli
Dlaczego hasła nie są widoczne podczas wpisywania ich w terminalu (np. przy sudo)?;Jest to funkcja bezpieczeństwa, aby ukryć długość i treść hasła przed osobami postronnymi.;odin_cli
Jaki jest skrót klawiszowy do KOPIOWANIA w terminalu (Linux/Windows)?;Ctrl + Shift + C;odin_cli
Jaki jest skrót klawiszowy do WKLEJANIA w terminalu (Linux/Windows)?;Ctrl + Shift + V;odin_cli
Co robi klawisz TAB w terminalu?;Automatycznie uzupełnia polecenia lub ścieżki do plików (Tab completion).;odin_cli
Do czego służy polecenie `whoami`?;Zwraca nazwę zalogowanego użytkownika.;odin_cli
Do czego służy polecenie `pwd`?;Wyświetla ścieżkę do katalogu, w którym aktualnie się znajdujesz (Print Working Directory).;odin_cli
Do czego służy polecenie `ls`?;Wyświetla zawartość (pliki i foldery) w bieżącym katalogu.;odin_cli
Do czego służy polecenie `cd` (Change Directory)?;Służy do nawigacji (przechodzenia) między folderami.;odin_cli
Gdzie przeniesie Cię samo polecenie `cd` (bez argumentów) lub `cd ~`?;Do katalogu domowego użytkownika (Home directory).;odin_cli
Gdzie przeniesie Cię polecenie `cd ..`?;Poziom wyżej, do katalogu nadrzędnego (Parent directory).;odin_cli
Jakie polecenie tworzy nowy katalog (folder)?;mkdir <nazwa_folderu>;odin_cli
Jakie polecenie tworzy nowy, pusty plik?;touch <nazwa_pliku>;odin_cli
Jak usunąć plik w terminalu?;rm <nazwa_pliku>;odin_cli
Jak zmienić nazwę pliku lub katalogu (lub go przenieść)?;mv <stara_nazwa> <nowa_nazwa>;odin_cli
Co oznacza kropka `.` w poleceniu takim jak `code .`?;Oznacza "bieżący katalog".;odin_cli
Jak otworzyć bieżący folder w VS Code z poziomu terminala?;code .;odin_cli
