Jaka jest różnica między `mousedown/mouseup` a `click`?;`click` odpala się dopiero po pełnym cyklu naciśnięcia i puszczenia przycisku myszy. `mousedown` odpala się natychmiast po wciśnięciu, a `mouseup` po puszczeniu.;odin_js_events
Jaka jest kluczowa różnica między `mouseover` a `mouseenter`?;`mouseover` BĄBELKUJE (odpala się, gdy wjedziesz na element LUB jego dzieci). `mouseenter` NIE bąbelkuje (odpala się tylko, gdy wjedziesz na sam element, ignoruje dzieci).;odin_js_events_details
W zdarzeniach klawiatury, jaka jest różnica między `event.key` a `event.code`?;`event.key` zwraca ZNAK (np. "a" lub "A" zależnie od Shift). `event.code` zwraca KOD FIZYCZNEGO KLAWISZA (np. "KeyA"), niezależnie od układu klawiatury czy wciśniętego Shifta.;odin_js_events
Na czym polega "Event Delegation" (Delegacja Zdarzeń)?;Na przypisaniu JEDNEGO event listenera do rodzica (np. listy `<ul>`), zamiast przypisywania listenerów do każdego dziecka (`<li>`) z osobna. Wykorzystuje mechanizm bąbelkowania (bubbling).;odin_js_patterns
Dlaczego Event Delegation jest wydajniejsza dla dynamicznych elementów?;Ponieważ nie musisz dodawać/usuwać listenerów za każdym razem, gdy dodajesz nowy element do listy. Rodzic "wyłapie" kliknięcia również na nowych dzieciach.;odin_js_patterns
W Event Delegation, czym różni się `e.target` od `e.currentTarget`?;`e.target` to element, który FAKTYCZNIE kliknięto (np. ikonka wewnątrz przycisku). `e.currentTarget` to element, do którego przypisano listener (np. sam przycisk lub kontener).;odin_js_patterns
Co robi metoda `element.dispatchEvent(event)`?;Ręcznie wyzwala (uruchamia) zdarzenie na danym elemencie (symuluje np. kliknięcie).;odin_js_events_advanced
Jak stworzyć proste, własne zdarzenie (Custom Event)?;`const myEvent = new Event('hello');` (lub `new CustomEvent` dla bardziej złożonych).;odin_js_events_advanced
Czym różni się `new CustomEvent` od `new Event`?;`CustomEvent` pozwala przekazać dodatkowe dane w drugim argumencie (w obiekcie `detail`).;odin_js_events_advanced
Jak przekazać dane (payload) w Custom Event?;`new CustomEvent('nazwa', { detail: { twojeDane: 123 } })`;odin_js_events_advanced
Jakie zdarzenie klawiatury odpala się ciągle, gdy trzymasz klawisz wciśnięty?;`keydown` (z właściwością `repeat: true`).;odin_js_events
Jakie są 3 fazy przepływu zdarzenia (Event Flow) w standardzie DOM?;1. Faza Przechwytywania (Capturing Phase - od góry do elementu). 2. Faza Celu (Target Phase). 3. Faza Bąbelkowania (Bubbling Phase - od elementu do góry).;odin_js_events_mechanics
Domyślnie `addEventListener` nasłuchuje w której fazie?;W fazie Bąbelkowania (Bubbling).;odin_js_events_mechanics
Jak zmusić `addEventListener` do nasłuchiwania w fazie Przechwytywania (Capturing)?;Przekazując trzeci argument jako `true` lub obiekt opcji: `{ capture: true }`.;odin_js_events_mechanics
Co robi metoda `e.preventDefault()`?;Blokuje domyślną akcję przeglądarki dla danego zdarzenia (np. powstrzymuje link przed otwarciem nowej strony lub formularz przed wysłaniem i przeładowaniem strony).;odin_js_events_mechanics
Czy `e.preventDefault()` zatrzymuje bąbelkowanie zdarzenia?;Nie. Zatrzymuje tylko domyślną akcję. Zdarzenie nadal wędruje w górę DOM (chyba że użyjesz też `stopPropagation`).;odin_js_events_mechanics
Dlaczego poniższy kod NIE usunie Event Listenera: `btn.removeEventListener('click', () => { ... })`?;Ponieważ funkcja strzałkowa/anonimowa tworzy nową referencję w pamięci. `removeEventListener` musi otrzymać DOKŁADNIE TĘ SAMĄ funkcję (nazwaną lub zapisaną w zmiennej), która była użyta w `add`.;odin_js_pitfalls
Jak dodać Event Listener, który wykona się TYLKO RAZ (i sam się usunie)?;Używając opcji `{ once: true }`: `btn.addEventListener('click', handler, { once: true })`.;odin_js_events_mechanics
Jaka jest różnica między zdarzeniami `input` a `change` (w polach tekstowych)?;`input` odpala się przy KAŻDEJ zmianie znaku (na żywo). `change` odpala się dopiero po "zatwierdzeniu" zmiany (np. gdy user kliknie poza pole - blur, lub wciśnie Enter).;odin_js_events_forms
Jaka jest różnica między zdarzeniem `DOMContentLoaded` a `load` (window)?;`DOMContentLoaded`: HTML został wczytany i drzewo DOM zbudowane (skrypty mogą działać), ale obrazki/style mogą się jeszcze ładować. `load`: Cała strona (wraz z obrazkami i stylami) została w pełni załadowana.;odin_js_lifecycle
Co wskazuje słowo kluczowe `this` wewnątrz standardowej funkcji (nie strzałkowej) użytej jako Event Handler?;Wskazuje na element, do którego przypięto listener (to samo co `e.currentTarget`).;odin_js_this
Co wskazuje słowo kluczowe `this` wewnątrz Funkcji Strzałkowej użytej jako Event Handler?;Wskazuje na kontekst zewnętrzny (leksykalny), czyli zazwyczaj `window` lub `undefined` (nie wskazuje na kliknięty element!). Dlatego w eventach często bezpieczniej używać `e.currentTarget`.;odin_js_this
