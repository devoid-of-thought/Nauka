Mamy kod: `.menu .item { color: red; }` oraz `.item { color: blue; }`. Jaki kolor tekstu otrzyma element z klasą "item"?;Czerwony. Pierwsza reguła ma dwie klasy (waga 2 klasy), druga tylko jedną (waga 1 klasa).;odin_css_scenarios
Mamy kod: `#sidebar { color: red; }` oraz `html body div.sidebar { color: blue; }`. Jaki kolor wygra?;Czerwony. ID (#) zawsze wygrywa, nawet jeśli druga reguła ma mnóstwo tagów i klas.;odin_css_scenarios
Mamy kod: `.box > .title { color: red; }` oraz `.box .title { color: blue; }`. Jaki kolor wygra?;Niebieski. Obie reguły mają po dwie klasy (kombinator `>` nie zmienia wagi), więc decyduje kolejność w pliku (ostatnia wygrywa).;odin_css_scenarios
Rodzic (Parent) ma ustawione `#container { color: red; }`. Dziecko (Child) wewnątrz ma ustawione `p { color: blue; }`. Jaki kolor będzie miało dziecko?;Niebieski. Bezpośrednie wskazywanie elementu (nawet słabym selektorem `p`) zawsze wygrywa z dziedziczeniem (nawet od silnego ID).;odin_css_scenarios
Czy styl zdefiniowany "liniowo" w HTML (`<div style="color: green">`) wygra ze stylem zdefiniowanym w pliku CSS z użyciem ID?;Tak. Style liniowe (Inline styles) mają wyższy priorytet niż jakiekolwiek selektory w pliku CSS.;odin_css_scenarios
Mamy kod: `ul li { color: red; }` oraz `.list-item { color: blue; }`. Jaki kolor wygra?;Niebieski. Jedna klasa jest silniejsza niż dwa tagi (elementy).;odin_css_scenarios
Ustawiasz `border: 1px solid black;` na elemencie `div`. Czy paragraf `<p>` wewnątrz tego diva odziedziczy ramkę?;Nie. Właściwości layoutu (jak border, margin, padding) nie są dziedziczone.;odin_css_scenarios
Mamy kod: `* { color: black; }` oraz `h1 { color: orange; }`. Jaki kolor będzie miał nagłówek h1?;Pomarańczowy. Selektor typu (`h1`) jest silniejszy niż selektor uniwersalny (`*`), który nie ma wagi.;odin_css_scenarios
Dlaczego `nav .link` ma taką samą specyficzność jak `nav > .link`?;Ponieważ znaki kombinatorów (spacja, >, +) nie dodają żadnych punktów do specyficzności. Liczą się tylko same nazwy selektorów (1 tag + 1 klasa w obu przypadkach).;odin_css_theory
