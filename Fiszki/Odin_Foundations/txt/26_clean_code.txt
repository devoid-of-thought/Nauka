Dlaczego "Czysty Kod" (Clean Code) jest ważny?;Ponieważ programiści spędzają znacznie więcej czasu na CZYTANIU kodu (swojego i innych) niż na jego pisaniu. Czysty kod jest łatwiejszy do zrozumienia i utrzymania.;odin_clean_code
Jaka jest zasada `camelCase` w nazewnictwie zmiennych?;Pierwsze słowo małą literą, każde kolejne wielką (np. `myVariableName`).;odin_clean_code
Dlaczego nazwy zmiennych takie jak `x`, `data`, `info` są złe?;Są zbyt ogólne i nie mówią nic o zawartości zmiennej. Wymagają zgadywania lub analizowania całego kodu.;odin_clean_code
Jak powinny zaczynać się nazwy zmiennych przechowujących dane (np. stringi, liczby)?;Od rzeczownika lub przymiotnika (np. `userName`, `isActive`, `playerScore`).;odin_clean_code
Jak powinny zaczynać się nazwy funkcji?;Od czasownika (akcji), np. `getUser`, `calculateScore`, `isLoggedIn`.;odin_clean_code
Co to są "Magic Numbers" (Magiczne Liczby) w kodzie i dlaczego należy ich unikać?;To liczby wpisane "na sztywno" w kodzie (np. `if (status === 2)`). Należy ich unikać, bo nie wiadomo co oznaczają. Lepiej przypisać je do nazwanej stałej (np. `const STATUS_ACTIVE = 2`).;odin_clean_code
Jaka jest konwencja nazewnictwa stałych (wartości, które są znane przed uruchomieniem programu i się nie zmieniają)?;WIELKIE_LITERY z podkreśleniami (SCREAMING_SNAKE_CASE), np. `const MAX_RETRY_COUNT = 5;`.;odin_clean_code
Jaka jest główna zasada dotycząca komentarzy w kodzie ("Why vs How")?;Komentarze powinny wyjaśniać DLACZEGO coś zostało zrobione (intencję, biznesowy powód), a nie JAK działa kod (to powinno wynikać z samej nazwy funkcji/zmiennej).;odin_clean_code
Dlaczego nie powinno się zostawiać w kodzie zakomentowanych fragmentów "na później"?;Ponieważ od śledzenia historii zmian i starego kodu jest GIT. Zakomentowany kod tylko zaśmieca plik.;odin_clean_code
Kiedy komentarz jest zbędny (Redundant Comment)?;Gdy opisuje to, co widać w kodzie gołym okiem (np. `let i = 0; // ustaw i na zero`).;odin_clean_code
Co zrobić, jeśli czujesz potrzebę napisania komentarza wyjaśniającego skomplikowany fragment kodu?;Spróbuj najpierw poprawić kod (Refactoring) – np. wydzielić ten fragment do osobnej funkcji z dobrą nazwą, która sama "opowie", co robi.;odin_clean_code
Jaka jest zalecana maksymalna długość linii kodu (orientacyjnie)?;Około 80 znaków (aby nie trzeba było przewijać ekranu w bok).;odin_clean_code
Dlaczego warto zweryfikować logikę (np. pseudokodem lub diagramem) ZANIM zaczniesz pisać właściwy kod?;Pozwala to wykryć błędy logiczne wcześniej i uniknąć pisania "na brudno", co oszczędza czas na późniejszym poprawianiu.;odin_clean_code_principles
Jakie kontenery w HTML powinny mieć nadane jasne nazwy (ID lub klasy)?;Główne kontenery strukturalne (np. header, footer, content-column), aby struktura strony była widoczna na pierwszy rzut oka.;odin_clean_code_principles
Jaki jest główny cel stosowania wcięć (Indentation) w kodzie?;Wizualne pokazanie, gdzie zaczyna się i kończy dany blok kodu lub element. Bez tego struktura jest nieczytelna.;odin_clean_code_principles
Kiedy jest najlepszy moment na pisanie komentarzy wyjaśniających kod?;W trakcie pisania kodu (gdy masz świeżą wiedzę na temat tego, jak on działa), a nie "kiedyś później".;odin_clean_code_principles
Czego NIE należy pisać w komentarzach (wg artykułu)? Wymień 2 przykłady.;1. Notatek dla siebie ("Dokończę to później"). 2. Obwiniania innych ("To kod Johna"). 3. Wyjaśniania oczywistości. 4. Zostawiania bloków zakomentowanego, martwego kodu.;odin_clean_code_principles
Co należy zrobić z funkcją, która urosła do np. 100 linii kodu?;Należy ją rozbić na mniejsze, bardziej wyspecjalizowane funkcje.;odin_clean_code_principles
Jeśli Twoja firma nie ma standardów nazewnictwa (np. prefixów dla zmiennych), co powinieneś zrobić?;Stworzyć własny, spójny system i się go trzymać, aby uniknąć zgadywania "co autor miał na myśli".;odin_clean_code_principles
Jaka jest złota zasada edytowania cudzego (lub starego) kodu?;Szanuj zastany styl. Jeśli dodajesz `if`, zachowaj istniejące wcięcia i styl komentarzy. Nie wprowadzaj chaosu.;odin_clean_code_principles
Dlaczego należy unikać mieszania języków (np. wpisywania stylów CSS inline w HTML lub bloków JS wewnątrz HTML)?;Powoduje to bałagan (Spaghetti Code) i utrudnia zarządzanie kodem. Logika, wygląd i struktura powinny być oddzielone.;odin_clean_code_principles
Dlaczego warto łączyć (summarize) wiele plików CSS/JS w mniejszą liczbę plików importowanych?;Każdy import to osobne zapytanie HTTP, które spowalnia ładowanie strony. Mniejsza liczba plików to lepsza wydajność i porządek.;odin_clean_code_principles
